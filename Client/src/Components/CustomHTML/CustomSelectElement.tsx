export interface SelectOptions {
    title : string, 
    value : string,
}

interface Props {
    props : string | number,
    opsList : string | number[],
}

export function CustomSelect({props, opsList} : Props){

    return(
        <>
        </>
    )

}